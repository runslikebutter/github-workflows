on:
  workflow_call:
    secrets:
      E2E_REPO_TOKEN: { required: true }

jobs:
  prep:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
        with:
          repository: runslikebutter/e2e
          token: ${{ secrets.E2E_REPO_TOKEN }}

      - name: Get Node Modules
        uses: ./.github/utils/cache
